[project]
name = "ftqp"
version = "0.1.0"
description = "MVP for a code-indexed probabilistic resource calculus for fault-tolerant quantum computing."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "z3-solver>=4.16.0.0",
]

[tool.uv]
package = false
